# Scripts

This folder contains the scripts and templates used to execute the workflow of the experiments:

* `launch_benchmark.sh.templ`: the template of the script running the benchmark application using the tested configuration
* `parse_output.sh`: the script used to transform [Renaissance][Renaiss]'s output format into a CSV

[Renaiss]: https://renaissance.dev/
